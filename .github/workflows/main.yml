name: Deploy master
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout source code
        uses: actions/checkout@v2

      - name: Generate deployment package
        run: zip -r deploy.zip . -x '*.git*'

      - name: Deploy to EB
        uses: einaregilsson/beanstalk-deploy@v21
        with:
          aws_access_key: AKIAZL2L24W45QCJC5GS
          aws_secret_key: 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
          application_name: My-React
          environment_name: My-React-env
          version_label: ${{ github.sha}}
          existing_bucket_name: elasticbeanstalk-ap-south-1-643867272633
          region: ap-south-1
          deployment_package: deploy.zip
