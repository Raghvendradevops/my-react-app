name: Deploy master
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout source code
        uses: actions/checkout@v2

      - name: Generate deployment package
        run: zip -r deploy.zip . -x '*.git*'

      - name: Deploy to EB
        uses: einaregilsson/beanstalk-deploy@v21
        with:
          aws_access_key: AKIASQQZGYJS2JYCYF7F
          aws_secret_key: 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
          application_name: my-react-application
          environment_name: My-react-application-env-1
          version_label: ${{ github.sha}}
          existing_bucket_name: elasticbeanstalk-ap-south-1-172925305445
          region: us-west-2
          deployment_package: deploy.zip
